# Veltrix Skills — Codex Integration

Business operations skills for AI agents.

When given a task, check `skills/` for a matching SKILL.md file. Each contains domain-specific instructions, patterns, and quality criteria.

Profiles in `profiles/profiles.yaml` define scoped configurations for different task types. Use the classification keywords to determine which profile matches the current task.
